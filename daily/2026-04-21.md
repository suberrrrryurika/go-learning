# 📅 第 24 天 - 文件操作

**日期**: 2026-04-21  
**阶段**: 第2 阶段  
**预计学习时长**: 3 小时

---

## 🎯 今日学习目标

1. 掌握核心概念和原理
2. 完成 4 个代码示例
3. 完成 4 个实战练习
4. 理解最佳实践

---

## 📚 理论学习

### 核心概念

Go 语言进阶需要掌握以下核心概念：

**1. 错误处理哲学**
- 错误是值
- 显式错误处理
- 不推荐异常

**2. 并发模型**
- CSP (Communicating Sequential Processes)
- goroutine 轻量级线程
- channel 通信机制

**3. 接口设计**
- 隐式实现
- 小接口原则
- 依赖倒置


---

## 💻 代码示例

### 示例 1: 基础示例

```go
package main

import (
    "fmt"
    "sync"
)

// goroutine 示例
func worker(id int, wg *sync.WaitGroup) {
    defer wg.Done()
    fmt.Printf("Worker %d 开始工作\n", id)
    // 模拟工作
    for i := 0; i < 3; i++ {
        fmt.Printf("Worker %d 处理任务 %d\n", id, i)
    }
    fmt.Printf("Worker %d 完成工作\n", id)
}

func main() {
    var wg sync.WaitGroup
    
    // 启动 3 个 worker
    for i := 1; i <= 3; i++ {
        wg.Add(1)
        go worker(i, &wg)
    }
    
    // 等待所有 worker 完成
    wg.Wait()
    fmt.Println("所有任务完成")
}
```

**运行结果**:
```
Worker 1 开始工作
Worker 2 开始工作
Worker 3 开始工作
Worker 1 处理任务 0
...
所有任务完成
```

---

### 示例 2: 进阶示例

```go
package main

import (
    "fmt"
    "sync"
)

// channel 示例
func channelDemo() {
    ch := make(chan int, 5)
    var wg sync.WaitGroup
    
    // 生产者
    wg.Add(1)
    go func() {
        defer wg.Done()
        for i := 0; i < 5; i++ {
            ch <- i
            fmt.Printf("发送：%d\n", i)
        }
        close(ch)
    }()
    
    // 消费者
    wg.Add(1)
    go func() {
        defer wg.Done()
        for val := range ch {
            fmt.Printf("接收：%d\n", val)
        }
    }()
    
    wg.Wait()
}

func main() {
    channelDemo()
}
```

---

## 🎯 实战练习

### 练习 1: 基础练习

**要求**: 完成以下功能

```go
package main

import (
    "fmt"
    "sync"
)

// 练习：实现一个并发安全的计数器
// 要求：
// 1. 支持 Increment() 方法
// 2. 支持 Decrement() 方法
// 3. 支持 GetValue() 方法
// 4. 使用 mutex 或 atomic 保证并发安全

type Counter struct {
    // TODO: 添加字段
}

func main() {
    var wg sync.WaitGroup
    counter := &Counter{}
    
    // 启动 10 个 goroutine，每个增加 100
    for i := 0; i < 10; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            for j := 0; j < 100; j++ {
                counter.Increment()
            }
        }()
    }
    
    wg.Wait()
    fmt.Printf("最终值：%d (期望：1000)\n", counter.GetValue())
}
```

---

## ✅ 今日检查清单

- [ ] 完成理论学习
- [ ] 阅读所有代码示例
- [ ] 运行并理解每个示例
- [ ] 完成练习 1
- [ ] 完成练习 2
- [ ] 完成练习 3
- [ ] 记录学习笔记
- [ ] 总结今日收获

---

## 📖 扩展阅读

1. [Go 官方文档](https://golang.org/doc/)
2. [Go by Example](https://gobyexample.com/)
3. [Effective Go](https://golang.org/doc/effective_go.html)
4. [Go Blog](https://go.dev/blog/)

---

## 💡 小贴士

1. **多动手**: 代码要亲手敲，不要只看
2. **理解原理**: 不仅要会用，还要知道为什么
3. **善用文档**: 遇到问题先查官方文档
4. **代码规范**: 使用 gofmt 格式化代码
5. **错误处理**: 不要忽略任何错误

---

**继续加油！明天学习第 25 天内容！** 🚀

**学习进度**: 24/90 (26%)
